# ورود به سیستم
برای ورود از طریق `/login.php` یا صفحه اصلی از اطلاعات زیر استفاده کنید:

**مدیر کل (دسترسی کامل):**
- نام کاربری: `admin`
- رمز عبور: `admin`

**همکاران (طراح/تدوینگر و ...):**
- نام کاربری: `ali` (یا `reza` یا `sara`)
- رمز عبور: `123456`
