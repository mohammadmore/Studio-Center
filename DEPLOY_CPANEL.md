# راهنمای استقرار روی هاست cPanel

این برنامه بهینه شده است تا به سادگی روی هاست اشتراکی cPanel که از **Setup Node.js App** پشتیبانی می‌کند اجرا شود. 

## ۱. آماده‌سازی دیتابیس
در این نسخه از SQLite به عنوان پایگاه داده محلی استفاده شده است. فایل دیتابیس به صورت خودکار با نام `database.sqlite` در مسیر پروژه ساخته می‌شود و نیازی به کانفیگ MySQL از سمت شما نیست. تمام داده‌ها در همین فایل ذخیره می‌شوند. 

## ۲. تنظیم محیط (cPanel)
1. وارد cPanel شوید.
2. به بخش **Setup Node.js App** بروید و روی **Create Application** کلیک کنید.
3. مقادیر زیر را وارد کنید:
   - **Node.js version**: آخرین نسخه توصیه‌شده (مثلاً 18 یا 20).
   - **Application mode**: `Production`
   - **Application root**: فولدری که کدهای پروژه در آن آپلود شده است (مثلاً `asva-studio`).
   - **Application URL**: آدرس سایت شما.
   - **Application startup file**: `dist/server.cjs` (حتما باید این فایل انتخاب شود).
4. **Environment variables** (متغیرهای محیطی) زیر را با کلیک روی `Add Variable` اضافه کنید:
   - `NODE_ENV`: `production`
   - `SESSION_SECRET`: یک رشته‌ی متنی طولانی و تصادفی (حداقل ۳۲ کاراکتر، مثلاً `my_super_secret_key_change_me`).
   - `FORCE_HTTPS`: `true` (اگر روی سایت SSL فعال است).
   - نیازی به تعریف `PORT` نیست؛ cPanel خودش پورت مناسب را تزریق می‌کند.
5. روی **Create** کلیک کنید.

## ۳. بیلد پروژه
پروژه برای اجرای نهایی باید بیلد شود. 
اگر در سیستم خودتان بیلد را انجام می‌دهید:
```bash
npm install
npm run build
```
سپس تمام فایل‌ها (شامل فولدر `dist`) را در cPanel آپلود کنید.
*توجه: فایل `package.json` و سایر فایل‌های استاتیک نیز باید همراه پوشه `dist` آپلود شوند.*

## ۴. راه‌اندازی SSL
توصیه می‌شود از SSL رایگان cPanel (مثل Let's Encrypt) استفاده کنید. پس از فعالسازی گواهینامه، حتماً مقدار متغیر `FORCE_HTTPS` را در پنل Node.js برابر `true` قرار دهید و اپلیکیشن را ری‌استارت کنید تا کوکی‌های نشست (session) به درستی در حالت `Secure` کار کنند.

## عیب‌یابی (Troubleshooting)
- اگر با خطای ۵۰۳ مواجه شدید، لاگ‌های اپلیکیشن (از طریق cPanel Node.js App) را بررسی کنید. معمولاً ناشی از ست نبودن `SESSION_SECRET` است که برنامه به سرعت متوقف می‌شود.
