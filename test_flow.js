const http = require('http');

const loginData = JSON.stringify({ username: 'admin', password: 'password' }); // Assuming password works or we get a session? No, maybe I don't know the password.
// But wait! If I don't know the password, how can I test?
